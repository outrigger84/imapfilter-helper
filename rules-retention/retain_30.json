{
  "name": "Retention » Retain-30 tier",
  "enabled": true,
  "priority": 100,
  "conditions": {
    "all": [
      {"has_keyword": "Retain-30"},
      {"age_days_gt": 30}
    ]
  },
  "actions": [
    {
      "type": "move",
      "target": "Deleted Messages"
    }
  ],
  "comments": [
    "Generic retention tier rule. Any message tagged with the 'Retain-30'",
    "keyword by a sort rule (via a set_keywords action) is moved to Deleted",
    "Messages once it's more than 30 days old.",
    "This rules directory is evaluated on its own via --rules-dir, never",
    "merged with rules/, so it never competes with sort rules for a match."
  ]
}
